FROM node:22-alpine
WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

# API needs src/lib for validation and achievement types
COPY server/ ./server/
COPY src/lib/ ./src/lib/

ENV NODE_ENV=production
ENV PORT=3030
ENV HOST=0.0.0.0

EXPOSE 3030
CMD ["node", "server/app.js"]
