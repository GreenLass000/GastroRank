# Siguiente Paso del Proyecto

## Siguiente bloque real
El siguiente bloque ya no es de implementación grande. Es únicamente:

1. disponer de un entorno con `node` y `npm`
2. ejecutar `npm run db:verify`
3. ejecutar `npm run lint`
4. ejecutar `npm run build`
5. hacer la validación manual final de flujos y PWA
6. corregir solo los defectos que aparezcan en esa pasada

## Qué no debería volver a hacerse
- no reabrir `docs/FINAL_STEP.md` como si todo siguiera pendiente
- no volver a auditar siete veces pantallas ya cerradas
- no rediseñar el shell ni cambiar el stack de mapas

## Foco de revisión
- edición real de entidades
- detalle al tocar tarjetas
- share público
- CSV
- instalación PWA
- ausencia de errores de compilación/lint

## Instrucción recomendada
`Lee docs/CHECKPOINT.md y ejecuta db:verify, lint, build y la validación manual final. Solo corrige lo que falle.`
